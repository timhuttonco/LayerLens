# macOS metadata
.DS_Store

# Node (generate_icons.js dependency, not needed in production)
node_modules/

# Python background removal tooling — not part of the extension
*.onnx
__pycache__/

# Local dev notes you don't want public
*.local.*

# Dev icons and prod backups — never commit these
icons/*-dev.png
icons/*-prod.png
